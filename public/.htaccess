RewriteEngine On
RewriteBase /

# api/ ディレクトリはリライトしない（PHPがそのまま実行される）
RewriteRule ^api/ - [L]

# 実ファイルが存在しない場合は /index.html にリライト（SPA ルーティング）
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
